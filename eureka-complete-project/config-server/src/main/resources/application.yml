server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: git@github.com:Jonata03/spring-config-server
          default-label: main
          search-paths:
          - 'greeting-service*'